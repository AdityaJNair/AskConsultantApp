import { connect } from 'react-redux'
import MessageView from "../../../components/messenger/MessengerBox/MessageView";

const MessageViewSmart = connect()(MessageView)

export default MessageViewSmart